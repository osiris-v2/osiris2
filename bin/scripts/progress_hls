#!/bin/bash
source '/etc/environment'
FILE="${OSIRIS000_BIN}/com/datas/ffmpeg/progress_hls.txt"
>$FILE
chmod 0777 $FILE
echo "PROGRESS HLS" > $FILE 
while true; do
echo "PROGRESS HLS"
clear
echo "PROGRESS HLS"
sleep 0.5
tail -n 10 -f $FILE
done
